{
  "name": "subkill",
  "version": "0.1.0",
  "description": "An agent that syncs with Gmail to detect, track, and manage subscription renewals — reminds you before they renew and can freeze the dedicated card if you don't respond.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "googleapis": "^144.0.0",
    "pg": "^8.12.0",
    "node-cron": "^3.0.3",
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express-session": "^1.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
